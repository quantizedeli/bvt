# Hero Scene Contracts

Her hero sahne:

1. `SceneData` üretir
2. `events` ile anlatı işaretleri taşır
3. poster ve interaktif çıktı verir
4. aynı veriden farklı backend'lere aktarılabilir

Temel alanlar:

- `t`
- `positions`
- `phases`
- `coherence`
- `order_param`
- `field_grid`
- `metrics`
